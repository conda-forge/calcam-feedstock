context:
  name: calcam
  version: "2.18.1"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  - url: https://github.com/euratom-software/${{ name }}/archive/refs/tags/${{ version }}.tar.gz
    sha256: c4e55a66cba2351cec82f08cafbbd33ceb4e9a42264fa15b93e5e9cf0c89fe90

build:
  noarch: python
  script: pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - calcam = calcam:start_gui
  number: 0

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools >=61.0
  run:
    - python >=${{ python_min }}
    - matplotlib-base
    - opencv
    - pyqt6
    - scipy
    - vtk-base >=7,!=9.1.*
    - h5py
    - triangle

tests:
  - python:
      imports:
        - calcam
      pip_check: false
      python_version: ${{ python_min }}.*

about:
  homepage: http://euratom-software.github.io/calcam/html/index.html
  summary: Spatial calibration tools for science & engineering camera systems (python module + GUI app).
  repository: https://github.com/euratom-software/calcam/
  documentation: http://euratom-software.github.io/calcam/html/index.html
  license: EUPL-1.1
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - ssilburn
    - munechika-koyo
